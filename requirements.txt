mcp~=1.0
google-genai~=1.0
streamlit~=1.35
spacy~=3.7
pydantic~=2.0          # used directly by agents/orchestrator.py (response_schema); pin explicitly
# spaCy language models are NOT pip deps; install per language, e.g.:
#   python -m spacy download pt_core_news_sm en_core_web_sm
# Sentence analysis degrades gracefully (LLM-only) when a model is absent.
